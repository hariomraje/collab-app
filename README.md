# CollabApp

## Setup Instructions

1. Clone the repo
git clone https://github.com/your-username/collab-app.git

2. Install dependencies
npm install

3. Run Metro
npx react-native start

4. Run Android
npx react-native run-android

5. Run iOS (Mac only)
cd ios && pod install && cd ..
npx react-native run-ios
